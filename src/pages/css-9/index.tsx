import "./style.less"

function index() {
  return (
    <div className="css-9">
      <div className="three-column-text">
        This is a bunch of text split into three columns using the CSS `columns` property. The text is equally
        distributed over the columns.
      </div>
    </div>
  )
}

export default index
